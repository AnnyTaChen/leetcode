import pandas as pd

def changeDatatype(students: pd.DataFrame) -> pd.DataFrame:
    df = pd.DataFrame(students)
    df['grade'] = df[['grade']].astype(int)#df[['grade']] grade那行變成dataFrame
    return df
