import pandas as pd

def createDataframe(student_data: List[List[int]]) -> pd.DataFrame:
    colunm_name = ['student_id','age']
    result = pd.DataFrame(student_data,columns = colunm_name)
    return result
